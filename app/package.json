{
  "name": "app",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  },
  "dependencies": {
    "three": "^0.136.0"
  }
}
